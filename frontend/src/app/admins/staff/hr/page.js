export default function StaffHrPage() {
  return (
    <div style={{ padding: 18, opacity: 0.75, color: 'var(--theme-text)' }}>
      <h2 style={{ margin: 0 }}>Attendance & HR</h2>
      <p style={{ marginTop: 8 }}>
        This page is wired into the Staff module navigation. Next step is implementing backend models/endpoints for staff clock-in/out and leave requests.
      </p>
    </div>
  );
}
