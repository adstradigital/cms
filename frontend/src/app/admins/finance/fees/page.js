import FeesPage from '@/components/Admin/Finance/Fees/FeesPage';

export const metadata = { title: 'Fees | Finance', description: 'Fee management — structure, collection, tracking, receipts, concessions, defaulters' };

export default function FeesRoute() {
  return <FeesPage />;
}
