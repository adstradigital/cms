import CourseSessions from '@/components/Admin/CourseSessions/CourseSessions';

export const metadata = {
  title: 'Course Sessions | Campus Management System',
  description: 'Plan, schedule, and track all academic course sessions for your institution.',
};

export default function SessionsPage() {
  return <CourseSessions />;
}
