import DonationsPage from '@/components/Admin/Finance/Donations/DonationsPage';

export const metadata = { title: 'Donations | Finance', description: 'Track donor contributions and generate donation receipts' };

export default function DonationsRoute() {
  return <DonationsPage />;
}
