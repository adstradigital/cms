import PayrollPage from '@/components/Admin/Finance/Payroll/PayrollPage';

export const metadata = { title: 'Payroll | Finance', description: 'Salary structures, payroll runs, and staff payslips' };

export default function PayrollRoute() {
  return <PayrollPage />;
}
